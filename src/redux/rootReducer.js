import { combineReducers } from 'redux';

/*
        combineReducers là một helper function được Redux cung cấp, 
    giúp bạn kết hợp nhiều reducer nhỏ thành một reducer tổng để đưa vào store.
*/
const rootReducer = combineReducers({});

export default rootReducer;